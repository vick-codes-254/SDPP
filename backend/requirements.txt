# ════════════════════════════════════════════════════════════════
#  SDPP — Secure Data Protection Platform — Runtime Dependencies
#  Pinned to compatible ranges for Python 3.13+
# ════════════════════════════════════════════════════════════════

# ── Web framework / ASGI ────────────────────────────────────────
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
python-multipart>=0.0.18        # multipart/form-data file uploads
email-validator>=2.2.0          # EmailStr validation

# ── Data validation / settings ──────────────────────────────────
pydantic>=2.10.0,<3.0
pydantic-settings>=2.7.0,<3.0

# ── Persistence ─────────────────────────────────────────────────
SQLAlchemy>=2.0.36,<2.1
alembic>=1.14.0,<2.0
asyncpg>=0.30.0                  # async PostgreSQL driver (runtime)
psycopg[binary]>=3.2.0          # sync PostgreSQL driver (alembic/migrations)

# ── Cryptography & security ─────────────────────────────────────
cryptography>=44.0.0            # AES-256-GCM, X.509, key derivation
argon2-cffi>=23.1.0             # Argon2id password hashing (reference impl)
passlib>=1.7.4                  # password hashing context abstraction
PyJWT[crypto]>=2.10.0           # JSON Web Tokens (RS256/HS256)

# ── Observability ───────────────────────────────────────────────
structlog>=24.4.0               # structured, tamper-evident logging
prometheus-client>=0.21.0       # security metrics export
